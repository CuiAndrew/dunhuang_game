# 《敦煌逃亡》验收记录

验证基线：`main`，Three.js r169 CDN import map，`npm test`。

| # | 验收项 | 结果 | 证据 |
| ---: | --- | --- | --- |
| 1 | 静态服务器启动无控制台错误 | 通过 | In-app Browser 加载 CDN r169 模块（上一版 `main.js?v=20260920-10`）后日志为空 |
| 2 | 开始界面与开始按钮 | 通过 | 浏览器点击后 `MENU → PLAYING`，覆盖层隐藏 |
| 3 | 自动前进、相机跟随、无穿模 | 通过 | `Runner`/`CameraRig` 单测 + 浏览器 HUD 距离增长 |
| 4 | A/D、箭头、滑动换道与边界 | 通过 | `tests/input.test.mjs`、`tests/entities.test.mjs`、`tests/track-graph.test.mjs`；浏览器验证 `←` 向屏幕左侧、`→` 向屏幕右侧移动 |
| 5 | 跳跃/滑铲、输入缓冲、禁止二段跳 | 通过 | `tests/entities.test.mjs` 固定时长动作断言 |
| 6 | 低栏/悬梁/立柱/火盆/断层 | 通过 | `CONFIG.spawn.obstacleTypes` 五类，GAP 跳跃回归测试，浏览器画面可见断层 |
| 7 | 每组至少保留一条车道或可跳跃路线 | 通过 | 物理障碍生成测试 + 全车道 GAP 跳跃测试 + 5 分钟确定性模拟 |
| 8 | 铜钱收集、HUD、粒子/音效反馈 | 通过 | `PickupSpawner` 收集测试、Score 测试、Fx/Sfx 接口测试 |
| 9 | 磁铁/护盾/加速与倒计时 | 通过 | `PowerUp` 三类型计时与重置测试；浏览器 HUD 显示护盾/磁铁 |
| 10 | 追兵距离与 3–5 次撞击结算 | 通过 | `Pursuer` 距离恢复、撞击扣减、死亡阈值测试 |
| 11 | 结算数值与最高分存档 | 通过 | `Score` localStorage 读写测试；`Screens.showResult` 展示历史最高分并在新纪录时高亮 |
| 12 | 再来一次完全重置 | 通过 | 障碍/拾取/环境池 reset 测试与主循环接线 |
| 13 | 标签页切换自动暂停 | 通过 | `visibilitychange` 接线，`GameState` PAUSED 转换测试 |
| 14 | 移动端触摸不滚动 | 通过 | touch/mouse listener `passive:false` 与 `preventDefault` 测试 |
| 15 | 5 分钟不衰减、池容量稳定 | 通过（确定性模拟） | 18,000 个固定步长、轨道/障碍/拾取/环境容量断言；真实设备 FPS 仍建议发布前实测 |
| 16 | 距离难度提升 | 通过 | 5 分钟模拟断言早期单车道、后期出现双车道，速度持续加速 |
| 17 | 敦煌主题场景与角色原语 | 通过 | `DunhuangTheme` 使用赭红长袍/金色光环 Runner、砂岩镇墓兽、沙丘/寺门/洞窟/石灯/经幡五类环境；`tests/art-audio.test.mjs` 断言主题变体和可动画双腿 |
| 18 | 障碍与道具语义形状 | 通过 | 障碍在初始化阶段预建梁/柱/火盆/断层/低栏组合；铜钱含方孔，护盾含藻井环，加速含朱砂印与飘带；运行时只切换节点 |
| 19 | 主题替换边界 | 通过 | `ThemeDefinition`/`ThemeRegistry` 契约、主题键纹理缓存和 Runner 工厂注入测试通过；上海外滩可在同一工厂接口替换，不进入碰撞/状态机 |
| 20 | 本次浏览器入口缓存版本 | 通过（既有场景流程）；本轮 v13 复验受环境限制 | 既有浏览器记录确认 `main.js?v=20260920-12` 的菜单、开始、暂停/继续/重开、方向键左右和 640×480 流程，且当时 warn/error 日志均为空；本轮入口已更新为 `main.js?v=20260921-13`。尝试复验时浏览器自动化会话被沙箱 TIOCSTI 限制中断，因此不宣称新的手动截图或日志证据。 |
| 21 | 敦煌 Q 版视觉与主题扩展验收 | 自动化通过；浏览器复验受环境限制 | Q 版 Runner/追兵/障碍/铜钱节点、云层与沙色道路使用主题令牌；HUD 采用语义徽章，菜单/暂停/结算统一 `paper-panel` 与 `seal-button`，`prefers-reduced-motion` 关闭装饰动画；`DUNHUANG_THEME` 仍通过 ThemeRegistry 注入，上海外滩可沿同一工厂边界替换；`npm test` 66/66、strict 设计审计 0 findings；浏览器连接因 TIOCSTI 沙箱限制未完成本轮手动复验。 |
| 22 | 本地敦煌插画场景、人物、道具与 HUD | 自动化通过（73/73）；实时页面复验受浏览器连接限制 | 主题资产清单覆盖 9:16 夜景、8 帧飞天跑步、跳跃/滑行姿态、硬币、4 类障碍、3 类道具和 HUD 原画；`AssetLoader` 逐项失败回退，Runner/障碍/道具保持既有 gameplay API；竖屏画布和 HUD 槽位、缓存版本由静态测试约束。严格设计审计 0 findings。图像已检查源素材；尝试连接浏览器自动化时会话启动被 TIOCSTI 沙箱限制中断，因此本条不声称游戏内截图或交互验证。入口：`main.js?v=20260923-1`。 |
| 23 | 音频与游戏状态生命周期 | 自动化通过（全量 `npm test` 77/77）；浏览器实时音频复验未执行 | PLAYING 自动启动程序化环境音乐，PAUSED/DEAD/切后台停止定时器和活跃音符并挂起 AudioContext；继续游戏恢复五声音阶循环；暂停期间切换静音不会重启音频。覆盖并发 resume/pause 竞态；入口：`main.js?v=20260923-2`。 |
| 24 | 分数与距离边框留白 | 自动化通过（全量 `npm test` 78/78）；浏览器截图复验受当前沙箱连接限制 | 使用 HUD 原画 PNG 尺寸和 9:16 画布比例计算两块绝对定位原画的占高；在移动端 50% 面板宽度下净间距不少于画布高的 0.75%，避免边框交叉。距离面板下移，入口：`main.js?v=20260923-3`。 |

## 可复现命令

```bash
npm test
python3 -m http.server 4173 --bind 127.0.0.1
```

浏览器验收重点：打开 `http://127.0.0.1:4173/`，点击“开始逃亡”，确认 HUD 距离增长；点击暂停确认覆盖层出现，再点击继续恢复；确认笔记本方向键 `←/→` 与屏幕左右方向一致。2026-09-20 冒烟记录：`MENU → PLAYING`，距离持续增长，`PLAYING → PAUSED → PLAYING`，重开后仍可进入 `PLAYING`，方向键左右无反向，控制台日志为空。
